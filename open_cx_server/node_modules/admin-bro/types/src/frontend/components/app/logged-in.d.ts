import React from 'react';
import { CurrentAdmin } from '../../../current-admin.interface';
declare type Props = {
    session: CurrentAdmin;
    paths: {
        logoutPath: string;
    };
};
declare const LoggedIn: React.FC<Props>;
export default LoggedIn;
