import React from 'react';
import { CurrentAdmin } from '../../../current-admin.interface';
import { VersionProps } from '../../../admin-bro-options.interface';
declare type PropsFromState = {
    versions: VersionProps;
    session: CurrentAdmin | null;
    paths: {
        logoutPath: string;
    };
};
declare const _default: import("react-redux").ConnectedComponent<React.FunctionComponent<PropsFromState>, Pick<PropsFromState, never>>;
export default _default;
